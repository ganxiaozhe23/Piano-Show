/* Offline vendor hook. The editor's default stage renderer is Canvas based so
   it works without network access. Projects may replace this file with a
   pinned Three.js ESM build without changing the editor API. */
export const REVISION = 'offline-canvas';
